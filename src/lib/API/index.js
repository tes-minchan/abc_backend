const upbitAPI   = require('./upbit');
const gopaxAPI   = require('./gopax');
const bithumbAPI = require('./bithumb');
const coinoneAPI = require('./coinone');

module.exports = {
  upbitAPI,
  gopaxAPI,
  bithumbAPI,
  coinoneAPI
}